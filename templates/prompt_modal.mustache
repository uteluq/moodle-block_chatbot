{{!
    @template block_uteluqchatbot/prompt_modal

    Prompt configuration modal for the TÉLUQ chatbot block.

    This template renders a modal dialog that allows instructors to create or modify
    the chatbot's system prompt for their course. Features include:
    - Dynamic title based on prompt existence (Add/Modify)
    - Large textarea for prompt content editing
    - Link to documentation guide for best practices
    - Form validation and submission handling
    - Responsive modal design

    Context variables:
    * has_prompt (bool) - Whether a prompt already exists for this course
    * prompt_text (string) - Current prompt content (empty if new prompt)

    Example context (JSON):
    {
        "has_prompt": true,
        "prompt_text": "You are a helpful teaching assistant for this course..."
    }

    @copyright 2025 UNIVERSITÉ TÉLUQ & Université GASTON BERGER DE SAINT-LOUIS
}}

<div id="promptModal" class="block_uteluqchatbot modal" style="display: none;">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    {{#has_prompt}}{{#str}}modify_prompt, block_uteluqchatbot{{/str}}{{/has_prompt}}
                    {{^has_prompt}}{{#str}}add_prompt, block_uteluqchatbot{{/str}}{{/has_prompt}}
                </h5>
                <button type="button" class="close" onclick="togglePromptModal()">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                        <path d="M19 6.41L17.59 5L12 10.59L6.41 5L5 6.41L10.59 12L5 17.59L6.41 19L12 13.41L17.59 19L19 17.59L13.41 12L19 6.41Z" fill="currentColor"/>
                    </svg>
                </button>
            </div>
            <div class="modal-body">
                <form id="promptform">
                    <!-- Guide pour les enseignants -->
                    <div class="form-group">
                        <p>
                            {{#str}}consult_guide, block_uteluqchatbot{{/str}}
                            <a href="https://docs.google.com/document/d/1JZOkdeZaAFJMhsxdb3SC28gel7S_PDgC/edit?usp=sharing&ouid=106953369477562214173&rtpof=true&sd=true" target="_blank" rel="noopener noreferrer">
                                {{#str}}guide_link, block_uteluqchatbot{{/str}}
                            </a>
                        </p>
                    </div>

                    <!-- Champ de contenu du prompt -->
                    <div class="form-group">
                        <label for="prompttext">{{#str}}prompt_content, block_uteluqchatbot{{/str}}</label>
                        <textarea id="prompttext" name="prompttext"
                                  class="form-input prompt-textarea"
                                  placeholder="{{#str}}write_prompt_here, block_uteluqchatbot{{/str}}"
                                  style="height: 300px;" required>{{prompt_text}}</textarea>
                    </div>

                    <div class="modal-footer">
                        <button type="button" class="btn-secondary" onclick="togglePromptModal()">{{#str}}cancel, block_uteluqchatbot{{/str}}</button>
                        <button type="submit" class="btn-primary">{{#str}}save, block_uteluqchatbot{{/str}}</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
